{
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.exe",
                "-g", 
                "-Wall",
                "-std=c++11"
            ]
        }
    ]
}